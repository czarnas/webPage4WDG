Global setup:

  Download and install Git
  git config --global user.name "Your Name"
  git config --global user.email czarnas@gmail.com
  Add your public key
  mkdir webPage4WDG
  cd webPage4WDG
  git init
  git remote add origin git@github.com:czarnas/webPage4WDG.git
  git push origin master
      
Generating a key
ssh-keygen -t rsa -C "[mail]"

Podstawowe komendy:
	dodajemy plik:
		git add <plik> <plik2> ...
		git commit -m "wiadomosc o zmianach"
		git push origin master "zatwierdzanie zmian"
	usuwamy plik
		git rm <plik> <plik2> ...
		git commit -m "wiadomosc"
		git push origin master
	sprawdzamy co zostalo zmienione
		git chekout
	jak wyedytowalismy plik to:
		git add <plik>
		git commit -m "wiadomosc"
		git push origin master
